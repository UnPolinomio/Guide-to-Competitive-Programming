#include <bits/stdc++.h>
#define fore(x, a, b) for (lli x = a, limit = b; x < limit; ++x)
using namespace std;
using lli = long long int;
using ii = pair<lli, lli>;
using vi = vector<lli>;

int main() {
  ;
  return 0;
}
